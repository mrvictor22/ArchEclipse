# ===========================================
# ArchEclipse .gitignore
# ===========================================
# Strategy: Ignore EVERYTHING by default, then whitelist specific files.
# This prevents personal files from being tracked accidentally.
#
# Personal folders automatically ignored (by /* rule):
#   - Documents/, Downloads/, Pictures/, Music/, Videos/
#   - Desktop/, Games/, dev_proyects/
#   - .cache/, .local/, .mozilla/, .steam/
#   - Any *.log, *.blend, or other personal files
#
# Rice folders in ~/ that ARE tracked (part of upstream):
#   - wofi/, wofifull/, mako/, swaylock/ (legacy, not in active use)
#   - waybar/, waybarBackUp/ (legacy, replaced by AGS)
#   - themes/, scripts/, configs/ (active, part of rice)
#   - hypr/, hyprland.conf, hyprpaper.conf (core rice)
# ===========================================

# Ignore all files by default
/*

!LICENSE

!.gitignore
!README.md

!.github

!.config/
.config/*

# Include hyprland config
!.config/hypr/
# Include kitty config
!.config/kitty/
# include ranger config
!.config/ranger/
# include btop config
!.config/btop/
# include aria2c config
!.config/aria2/
# include pipwire config
!.config/pipewire/
# include ags
!.config/ags/
!.config/agsV1/
# include wayvnc config
!.config/wayvnc/
# include fastfetch config
!.config/fastfetch/
# wallpapers
!.config/wallpapers/
# Ignore discretion/SFW wallpapers (personal collection from Wallhaven API)
.config/wallpapers/discretion/
# lossless scaling config (frame generation)
!.config/lsfg-vk/
# include starship config
!.config/starship.toml

# include icons (cursort theme)
!.icons/

# include zsh config
!.zshrc
